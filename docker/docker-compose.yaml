version: '3.2'
services:
  postgres:
    image: postgres:11.3-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: shorten_url
      POSTGRES_PASSWORD: shorten_url
      POSTGRES_DB: shorten_url
